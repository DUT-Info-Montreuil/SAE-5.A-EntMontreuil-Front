export interface TD {
  id: number;
  name: string;
  id_Promotion: number;
  // Add other properties that a TD might have
  // For example, if you have a description or a date field, add them here
  // description?: string;
  // date?: Date;
}
